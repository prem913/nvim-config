-- local config = {
--   cmd = { vim.fn.expand("~/.local/share/nvim/mason/bin/jdtls") },
--   -- root_dir = vim.fs.dirname(vim.fs.find({ 'gradlew', 'mvnw' }, { upward = true })[1]),
--   -- works for for single project workspaces
--   root_dir = vim.fs.dirname(vim.fs.find({ 'build.gradle'})[1]),
-- }
--
-- print("Root dir detected : " , config.root_dir)
-- require('jdtls').start_or_attach(config)


-- Using config from lsp-config
